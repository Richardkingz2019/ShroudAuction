# vitest 4 pulls in a @vitejs/devtools-* peer graph that makes npm 10's
# arborist throw "Cannot read properties of null (reading 'edgesOut')" while
# building the ideal tree. Resolving peers the legacy way installs the same
# dependency set without that crash.
legacy-peer-deps=true
